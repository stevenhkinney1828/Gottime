# This repo is authored on Windows and built/tested on macOS + Linux CI runners — pin line
# endings to LF explicitly rather than leaving it to each contributor's core.autocrlf setting.
* text=auto eol=lf
